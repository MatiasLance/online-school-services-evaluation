services:
    app:
      container_name: osse-project
      build:
        context: .
        dockerfile: Dockerfile.prod
      restart: unless-stopped
      volumes:
        - .:/var/www/html
        - ./sessions:/var/lib/php/sessions
      networks:
        - osse-network

networks:
  osse-network:
    external: true